1. Hvað gera eftirfarandi Linux skipanir?
	cd -----------change directory
	cd ~ ---------change to home directory
	ls -----------show list
	pwd ----------show current directory
	mkdir --------make new folder
2. Hvað gera eftirfarandi git skipanir og hvers vegna eru þær gagnlegar?
	git clone ----- clone a repository into a new directory
	git log ------- show commit logs
	git status----- Show the working tree status
	git diff------- Show changes between commits, commit and working   
	tree, etc
	git checkout--- Switch branches or restore working tree files
3. Hver er munurinn á eftirfarandi git skipunum:
	a) git diff -------------------- is just a name of command
	b) git diff --staged -----------This form is to view the changes --------------------------------you made relative to the index
	c) git diff commit1 commit2-----This is to view the changes between --------------------------------two arbitrary <commit>
4. 	Hvað er útgáfustýring (e.version control)? 
-------version control system is for tracking changes in computer files -------and coordinating work on those files among mutliple people
5.	Hverjir eru helstu kostir við að nota GIT?
-------git allows a team of people to work together, all using the same -------files
6.	Hversu oft telur þú að eigi að gera commit í verkefni, rökstuddu?
-------Everytime after clearing a task
7.	Hvað er "Working directory"?" "Staging area"?" og "Repository" í 
GIT?
----a)Its is wherever you have checkout the project
----b) The stagin area is a file, generally contained in your Git directory, that stores information about what will go into your next commit.
----c)The purpose of Git is to manage a project, or a set of files, as they change over time. Git stores this information in a data structure called a repository
8.	Hvenær er sniðugt að nota greinar (branches)? 
------i don't know, never used it before
9.	Hver fann upp GIT?
10.	Hvar er GIT helst notað?